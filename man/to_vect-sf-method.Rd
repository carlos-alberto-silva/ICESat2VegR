% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_vect.R
\name{to_vect,sf-method}
\alias{to_vect,sf-method}
\title{Convert sf to terra::SpatVector}
\usage{
\S4method{to_vect}{sf}(x, target_crs = NULL, ...)
}
\arguments{
\item{x}{An \code{sf} object (POINT geometry recommended)}

\item{target_crs}{Optional CRS string (e.g., "EPSG:4326" or proj4) to reproject the output.}

\item{...}{ignored}
}
\value{
\code{terra::SpatVector}
}
\description{
Accepts an sf POINT layer (like \code{s} below) and returns a \code{terra::SpatVector}
while preserving all attributes and CRS. Optionally reprojects to a target CRS.
}
\examples{
# Example: s is an sf object with POINT geometry and fields like:
#   segment_id, beam, longitude, latitude, h_canopy_gt0, ..., geometry
# head(s)
# Simple feature collection with 6 features and 14 fields
# Geometry type: POINT
# Dimension:     XY
# Bounding box:  xmin: -84.02171 ymin: 31.29891 xmax: -84.02025 ymax: 31.31945
# Geodetic CRS:  WGS 84
# ...
# Convert to SpatVector (keep attributes + CRS)
# v <- to_vect(s)
# terra::plot(v, col = v$h_canopy_gt0)
}
\keyword{internal}
