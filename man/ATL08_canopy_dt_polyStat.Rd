% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ATL08_canopy_dt_polyStat.R
\name{ATL08_canopy_dt_polyStat}
\alias{ATL08_canopy_dt_polyStat}
\title{Statistics of ATL03 and ATL08 joined photons attributes within a given area}
\usage{
ATL08_canopy_dt_polyStat(atl08_terrain_dt, func, poly_id)
}
\arguments{
\item{atl08_canopy_dt}{An S4 object of class "icesat2.ATL08_canopy_dt" containing ATL08 canopy attributes
(output of [ATL08_canopy_attribute_dt()] function).}

\item{func}{The function to be applied for computing the defined statistics}

\item{poly_id}{Polygon id. If defined, statistics will be computed for each polygon}
}
\value{
Returns an S4 object of class [rICESat2Veg::icesat2.ATL08_canopy_dt]
Containing Statistics of ATL08 classified canopy photons
}
\description{
Computes a series of statistics ATL03 and ATL08 joined photons attributes within
area defined by a polygon
}
\examples{
# Specifying the path to ATL08 file (zip file)
outdir = tempdir()
atl08_zip <- system.file("extdata",
                  "ATL08_20220401221822_01501506_005_01.zip",
                  package="rICESat2Veg")

# Unzipping ATL08 file
atl08_path <- unzip(atl08_zip,exdir = outdir)

# Reading ATL08 data (h5 file)

# Extracting ATL08-derived canopy Metrics
atl08_canopy_dt<-ATL08_canopy_attributes_dt(atl08_h5=atl08_h5)

# Specifying the path to shapefile
polygon_filepath <- system.file("extdata", "polygon.shp", package = "rICESat2Veg")

# Reading shapefile as sf object
polygon <- terra::vect(polygon_filepath)

# Clipping ATL08 canopy attributes by Geometry
atl08_canopy_dt_clip <- ATL08_canopy_dt_clipGeometry(atl08_canopy_dt, polygon, split_by = "FID")

# Computing the max h_canopy by polygon id
max_h_canopy <-ATL08_canopy_dt_polyStat(atl08_canopy_dt_clip, func=max(h_canopy),poly_id="poly_id")
head(max_h_canopy)

# Define your own function
mySetOfMetrics <- function(x) {
  metrics <- list(
    min = min(x), # Min of x
    max = max(x), # Max of x
    mean = mean(x), # Mean of x
    sd = sd(x) # Sd of x
  )
  return(metrics)
}

# Computing a series of canopy statistics from customized function
h_canopy_metrics <-ATL08_canopy_dt_polyStat(ATL08_canopy_dt, func=mySetOfMetrics(h_canopy),poly_id="poly_id")
head(h_canopy_metrics)

close(atl03_h5)
close(atl08_h5)
}
