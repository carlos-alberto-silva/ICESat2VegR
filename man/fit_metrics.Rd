% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_metrics.R
\name{fit_metrics}
\alias{fit_metrics}
\title{Model fit metrics}
\usage{
fit_metrics(
  observed,
  predicted,
  plotstat = FALSE,
  legend = "topleft",
  unit = "",
  ...
)
}
\arguments{
\item{observed}{Numeric vector of observed values.}

\item{predicted}{Numeric vector of predicted values (same length/order as \code{observed}).}

\item{plotstat}{Logical; if TRUE, draws a 1:1 plot with the regression line. Default: FALSE.}

\item{legend}{Character position for the plot legend (e.g., "topleft"). Default: "topleft".}

\item{unit}{Character indicating the unit for RMSE/MAE/Bias (e.g., "Mg/ha"). Default: "".}

\item{...}{Additional arguments passed to \code{\link[graphics:plot.default]{graphics::plot()}}.}
}
\value{
A data.frame with rows for \code{rmse}, \code{rmseR} (\%), \code{mae}, \code{maeR} (\%),
\code{bias}, \code{biasR} (\%), \code{r}, and \code{adj_r2}.
}
\description{
Computes RMSE (absolute and relative), MAE (absolute and relative),
bias (absolute and relative), Pearson correlation (r), and adjusted \eqn{R^2} from a
linear fit between predicted and observed values. Optionally draws a 1:1 plot
with the regression line.
}
\examples{
observed  <- c(178, 33, 60, 80, 104, 204, 146)
predicted <- c(184, 28.5, 55, 85, 105, 210, 155)
fit_metrics(observed, predicted,
            plotstat = TRUE, legend = "topleft", unit = "Mg/ha",
            xlab = "Observed AGBD (Mg/ha)", ylab = "Predicted AGBD (Mg/ha)", pch = 16)
}
