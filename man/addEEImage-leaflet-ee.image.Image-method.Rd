% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addEEImage.R
\name{addEEImage,leaflet,ee.image.Image-method}
\alias{addEEImage,leaflet,ee.image.Image-method}
\title{Adds Earth Engine Image class to leaflet}
\usage{
\S4method{addEEImage}{leaflet,ee.image.Image}(
  map,
  x,
  bands,
  min_value = 0,
  max_value = 1,
  palette = c("#d55e00", "#cc79a7", "#f0e442", "#0072b2", "#009e73"),
  ...
)
}
\arguments{
\item{map}{\code{\linkS4class{leaflet}}. A leaflet class map.}

\item{x}{Earth Engine Image open with \code{ee$Image}.}

\item{min_value}{The minimum value to represent for visualization purposes}

\item{max_value}{The maximum value to represent for visualization purposes}

\item{palette}{A \code{\linkS4class{character}} describing a list of colors in either
hexadecimal or valid CSS color names.}

\item{...}{Other parameters to be passed to \code{\link[leaflet:map-layers]{leaflet::addTiles()}} function.}
}
\value{
A \code{\linkS4class{leaflet}}/\code{\linkS4class{htmlwidget}} with the Earth Engine image added.
}
\description{
Adds Earth Engine Image class to leaflet
}
\examples{
shp <- system.file("extdata/all_boudary.shp", package = "ICESat2VegR")

geom <- terra::vect(shp)
bbox <- terra::ext(geom)

center <- terra::geom(terra::centroids(terra::vect(bbox)))

aoi <- ee$Geometry$BBox(
  west = bbox$xmin,
  south = bbox$ymin,
  east = bbox$xmax,
  north = bbox$ymax
)

collection_id <- "NASA/HLS/HLSL30/v002"

ee_collection <- ee$ImageCollection(collection_id)

image <- c(ee_collection$filterDate("2019-04-01", "2019-07-31"),ee_collection$filterDate("2020-04-01", "2020-07-31"))
)$median()

library(leaflet)
leaflet() \%>\%
  addEEImage(
    image,
    bands = list(3, 2, 1),
    min_value = 0.001,
    max_value = 0.2
  ) \%>\%
  setView(lng = center[, "x"][[1]], lat = center[, "y"][[1]], zoom = 8)
}
