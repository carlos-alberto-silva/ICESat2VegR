% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ATL08_h5_clip.R
\name{ATL08_h5_clip}
\alias{ATL08_h5_clip}
\title{Clips ICESat-2 ATL08 H5 data}
\usage{
ATL08_h5_clip(
  atl08,
  output,
  clip_obj,
  landSegmentsMask_fn,
  beam = c("gt1r", "gt2r", "gt3r", "gt1l", "gt2l", "gt3l"),
  additional_groups = c("METADATA", "orbit_info", "quality_assessment",
    "atlas_impulse_response", "ancillary_data")
)
}
\arguments{
\item{output}{character. Path to the output h5 file.}

\item{beam}{\code{\linkS4class{character}}. The vector of beams to include, default
all c("gt1l", "gt2l", "gt3l", "gt1r", "gt2r", "gt3r")}

\item{additional_groups}{\code{\linkS4class{character}}. Other addional groups that should be included, default
c("METADATA", "orbit_info", "quality_assessment", "ancillary_data")}

\item{x}{\code{\linkS4class{icesat2.atl08_h5}} object,
obtained through \code{\link[=ATL08_read]{ATL08_read()}} for clipping}

\item{bbox}{\code{\linkS4class{numeric}} or \code{\link[terra:SpatExtent-class]{terra::SpatExtent}} for clipping, the
order of the bbox is the default from NASA's ICESat-2 CMS searching:
\link{ul_lat, ul_lon, lr_lat, lr_lon}.}
}
\value{
Returns the clipped S4 object of class \code{\linkS4class{icesat2.atl08_h5}}
}
\description{
This function clips ATL08 HDF5 file within beam groups,
but keeps metada and ancillary data the same.
}
\examples{
# Specifying the path to ATL08 file (zip file)
outdir <- tempdir()
atl08_zip <- system.file("extdata",
  "atl08_20220401221822_01501506_005_01.zip",
  package = "rICESat2Veg"
)

# Unzipping ATL08 file
atl08_path <- unzip(atl08_zip, exdir = outdir)

# Reading ATL08 data (h5 file)
atl08_h5 <- atl08_read(atl08_path = atl08_path)


# Bounding rectangle coordinates
xmin <- -107.7
xmax <- -106.5
ymin <- 32.75
ymax <- 42.75

# Clipping ATL08 photons  by boundary box extent
atl08_photons_dt_clip <- ATL08_h5_clipBox(atl08_h5, outdir, xmin, xmax, ymin, ymax)

close(atl08_h5)
}
