% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ATL03_ATL08_seg_cover_dt_compute.R,
%   R/ATL08_seg_cover_dt_compute.R
\name{ATL03_ATL08_seg_cover_dt_compute}
\alias{ATL03_ATL08_seg_cover_dt_compute}
\title{Compute segments id for a given segment length}
\usage{
ATL08_read(atl08_path)

ATL03_ATL08_seg_cover_dt_compute(
  atl03_atl08_seg_dt,
  func,
  ph_class = c(2, 3),
  beam = c("gt1l", "gt1r", "gt2l", "gt2r", "gt3l", "gt3r"),
  quality_ph = 0,
  night_flag = 1
)
}
\arguments{
\item{func}{The function to be applied for computing the defined statistics}

\item{ph_class}{Character vector indicating photons to process based
on the classification (1=ground, 2=canopy, 3=top canopy),
Default is c(2,3)}

\item{beam}{Character vector indicating beams to process. Default is
c("gt1l", "gt1r", "gt2l", "gt2r", "gt3l", "gt3r")}

\item{quality_ph}{Indicates the quality of the associated photon.
0 = nominal, 1 = possible_afterpulse, 2 = possible_impulse_response_
effect, 3=possible_tep. Default is 0}

\item{night_flag}{Flag indicating the data were acquired in night conditions: 0=day, 1=night. Default is 1}

\item{reflectance_ratio}{Numeric. The reflectance ratio to use to compute the coverage metric.
\mjseqn{\rho_v / \rho_g}, where \mjseqn{\rho_v} is the vegetation reflectance
and \mjseqn{\rho_v} is the ground reflectance. Default is 1 (same reflectance).}

\item{atl03_atl08_dt}{An S4 object of class "icesat2.atl03atl08_dt" containing ATL03 and ATL08 data
(output of \code{\link[=ATL03_ATL08_photons_attributes_dt_join]{ATL03_ATL08_photons_attributes_dt_join()}} function).}

\item{seg_length}{Segment length. Default is 30 m}
}
\value{
Returns an S4 object of class \link{"icesat2.atl03_atl08_dt-class"} containing ICESat-2 ATL08 data.

Returns an S4 object of class \code{\linkS4class{icesat2.atl08_dt}}
Containing Statistics of ATL03 and ATL08 labeled photons
}
\description{
This function reads the ICESat-2 Land and
Vegetation Along-Track Products (ATL08) as h5 file. \loadmathjax

Computes a series of statistics from ATL03 and ATL08 labeled photons
within a given segment length.
}
\details{
Coverage is calculated with the formula
\mjsdeqn{\frac{1}{1 + \frac{\rho_v N_g}{\rho_g N_v}}}
}
\examples{
# Specifying the path to ICESat-2 ATL08 data (zip file)
outdir <- tempdir()
atl08_fp_zip <- system.file("extdata",
  "ATL0802_A_2019108080338_O01964_T05337_02_001_01_sub.zip",
  package = "rICESat2Veg"
)

# Unzipping ICESat-2 ATL08 data
atl08_path <- unzip(atl08_fp_zip, exdir = outdir)

# Reading ICESat-2 ATL08 data (h5 file)
atl08 <- ATL08_read(atl08_path = atl08_path)

close(atl08)
# Specifying the path to ATL03 and ATL08 file (zip file)
outdir = tempdir()
atl03_zip <- system.file("extdata",
                  "ATL03_20220401221822_01501506_005_01.zip",
                  package="rICESat2Veg")

atl08_zip <- system.file("extdata",
                  "ATL08_20220401221822_01501506_005_01.zip",
                  package="rICESat2Veg")

# Unzipping ATL03 file
atl03_path <- unzip(atl03_zip,exdir = outdir)

# Unzipping ATL08 file
atl08_path <- unzip(atl08_zip,exdir = outdir)

# Reading ATL03 data (h5 file)
atl03_h5<-ATL08_read(atl03_path=atl03_path)

# Reading ATL08 data (h5 file)
atl08_h5<-ATL08_read(atl08_path=atl08_path)

# Extracting ATL03 and ATL08 labeled photons
atl03_atl08_dt<-ATL03_ATL08_photons_attributes_dt_join(atl03_h5,atl08_h5)
head(atl03_atl08_dt)

# Computing the max canopy height at 30 m segments
max_canopy <-ATL03_ATL08_compute_seg_attributes_dt_segStat(atl03_atl08_dt, func=max(ph_h),
                               seg_length = 30,
                               ph_class=c(2,3),
                               beam=c("gt1l", "gt1r", "gt2l", "gt2r", "gt3l", "gt3r"),
                               quality_ph=0,
                               night_flag=1)

head(max_canopy)

# Define your own function
mySetOfMetrics <- function(x) {
  metrics <- list(
    min = min(x), # Min of x
    max = max(x), # Max of x
    mean = mean(x), # Mean of x
    sd = sd(x) # Sd of x
    h_canopy = quantile(x,0.98)
  )
  return(metrics)
}

# Computing a series of canopy height statistics from customized function
canopy_metrics <- ATL03_ATL08_compute_seg_attributes_dt_segStat(atl03_atl08_dt, func=mySetOfMetrics(ph_h),
                               seg_length = 30, # 30 m
                               ph_class=c(2,3),
                               beam=c("gt1l", "gt1r", "gt2l", "gt2r", "gt3l", "gt3r"),
                               quality_ph=0,
                               night_flag=1)

head(canopy_metrics)

close(atl03_h5)
close(atl08_h5)
}
\seealso{
\url{https://icesat-2.gsfc.nasa.gov/sites/default/files/page_files/ICESat2_ATL08_ATBD_r006.pdf}
}
