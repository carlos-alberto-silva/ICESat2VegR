% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ATL03_ATL08_photons_attributes_dt_polyStat.R
\name{ATL03_ATL08_photons_attributes_dt_polyStat}
\alias{ATL03_ATL08_photons_attributes_dt_polyStat}
\title{Statistics of ATL03 and ATL08 joined photons attributes within a given area}
\usage{
ATL03_ATL08_photons_attributes_dt_polyStat(atl03_atl08_dt, func, seg_length,ph_class,beam,quality_ph,night_flag)
}
\arguments{
\item{atl03_atl08_dt}{An S4 object of class \link{icesat2.atl08_dt} containing ATL03 and ATL08 data
(output of \code{\link[=ATL03_ATL08_photons_attributes_dt_join]{ATL03_ATL08_photons_attributes_dt_join()}} function).}

\item{func}{The function to be applied for computing the defined statistics}

\item{poly_id}{Polygon id. If defined, statistics will be computed for each polygon}
}
\value{
Returns an S4 object of class \link{icesat2.atl08_dt}
Containing Statistics of ATL08 classified canopy photons
}
\description{
Computes a series of statistics ATL03 and ATL08 joined photons attributes within
area defined by a polygon
}
\examples{

# Specifying the path to ATL03 and ATL08 file (zip file)
outdir = tempdir()
atl03_zip <- system.file("extdata",
                  "ATL03_20220401221822_01501506_005_01.zip",
                  package="ICESat2VegR")

atl08_zip <- system.file("extdata",
                  "ATL08_20220401221822_01501506_005_01.zip",
                  package="ICESat2VegR")

# Unzipping ATL03 file
atl03_path <- unzip(atl03_zip,exdir = outdir)

# Unzipping ATL08 file
atl08_path <- unzip(atl08_zip,exdir = outdir)

# Reading ATL03 data (h5 file)

# Reading ATL08 data (h5 file)

# Extracting ATL03 and ATL08 photons and heights
atl03_atl08_dt<-ATL03_ATL08_photons_attributes_dt_join(atl03_h5,atl08_h5)
head(atl03_atl08_dt)

# Specifying the path to shapefile
polygon_filepath <- system.file("extdata", "polygon.shp", package = "ICESat2VegR")

# Reading shapefile as sf object
polygon <- terra::vect(polygon_filepath)

# Clipping ATL08 terrain attributes by Geometry
atl03_atl08_dt_clip <- ATL03_ATL08_joined_dt_clipGeometry(atl03_atl08_dt, polygon, split_by = "FID")

# Computing the maximum ph_h by polygon id
max_ph_h <-ATL03_ATL08_photons_attributes_dt_polyStat(atl03_atl08_dt_clip, func=max(ph_h),poly_id="poly_id")
head(max_ph_h)

# Define your own function
mySetOfMetrics <- function(x) {
  metrics <- list(
    min = min(x), # Min of x
    max = max(x), # Max of x
    mean = mean(x), # Mean of x
    sd = sd(x) # Sd of x
  )
  return(metrics)
}

# Computing a series of ph_h statistics from customized function
ph_h_metrics <-ATL03_ATL08_photons_attributes_dt_polyStat(atl03_atl08_dt, func=mySetOfMetrics(ph_h),poly_id="poly_id")
head(ph_h_metrics)

close(atl03_h5)
close(atl08_h5)
}
