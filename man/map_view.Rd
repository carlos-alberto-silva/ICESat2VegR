% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_tools.R
\name{map_view}
\alias{map_view}
\title{Compose a leaflet map from multiple layers (EE rasters and/or vectors)}
\usage{
map_view(
  layers,
  base_tiles = c("OSM", "Carto.Light", "Carto.Dark"),
  add_layers_control = TRUE,
  add_opacity_controls = TRUE,
  fit_to = NULL
)
}
\arguments{
\item{layers}{A list of layer specs. For rasters: \verb{list(type="ee_image", x, bands, aoi=NULL, group=NULL, min_value=0, max_value=1, palette=NULL, is_class=FALSE, scale_to_int=TRUE, int_factor=1000L, tile=FALSE, nx=NULL, ny=NULL, legend=list(title=NULL, position=\\"bottomright\\", opacity=1, auto=NULL, probs=c(2,98), scale=NULL))}.
For vectors: \code{list(type="vector", vect, group=NULL, border_color, border_weight, fill, fill_color, fill_opacity, color_field, palette, legend_title)}.}

\item{base_tiles}{One of \code{"OSM"}, \code{"Carto.Light"}, \code{"Carto.Dark"}.}

\item{add_layers_control}{Logical; add layers control panel.}

\item{add_opacity_controls}{Logical; add per-group opacity sliders.}

\item{fit_to}{Optional EE Rectangle to fit the initial view.}
}
\value{
A \code{leaflet} htmlwidget.
}
\description{
High-level map composer capable of adding Earth Engine raster
tiles (optionally tiled by AOI grid) and vector overlays (\code{sf}/\code{SpatVector}),
with layer control, legends, and per-group opacity sliders.
}
\examples{
\dontrun{
m <- map_view(list(
  list(type="ee_image", x=img, bands="prediction_layer", aoi=aoi,
       group="Prediction", min_value=0, max_value=30,
       legend=list(title="Height (m)", auto="quantile")),
  list(type="vector", vect=polys_sf, group="Sites", color_field="site")
))
}
}
