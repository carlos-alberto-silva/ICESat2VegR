% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ATLAS_dataDownload.R
\name{earthdata_login}
\alias{earthdata_login}
\title{Get or Create a \code{.netrc} File for NASA Earthdata Login}
\usage{
earthdata_login(output_dir = "~")
}
\arguments{
\item{output_dir}{Character. Directory where the \code{.netrc} file should be
created.}
}
\value{
Character string. The normalized path to the \code{.netrc} file
being used.
}
\description{
Creates (if necessary) and configures a \code{.netrc} file containing
credentials for \strong{NASA Earthdata Login} (<urs.earthdata.nasa.gov>).
This file is required for authenticated downloads from Earthdata
services (e.g., via \code{earthaccess}, \code{curl}, \code{wget}, or other clients).
}
\details{
The function will:
\itemize{
\item Use an existing \code{.netrc} file if valid credentials are already present
\item Otherwise prompt the user securely for credentials
\item Or read credentials from environment variables:
\itemize{
\item \code{EARTHDATA_USERNAME}
\item \code{EARTHDATA_PASSWORD}
}
\item Set the \code{NETRC} environment variable for the active Python session
}

Credentials are written in standard \code{.netrc} format:

\if{html}{\out{<div class="sourceCode text">}}\preformatted{machine urs.earthdata.nasa.gov
  login <username>
  password <password>
}\if{html}{\out{</div>}}

If credentials are not found in environment variables and the
package \strong{getPass} is available, the user is prompted securely.
Otherwise, an error is thrown.

The function also updates the \code{NETRC} environment variable inside
the active Python session via \strong{reticulate}, ensuring compatibility
with Python libraries such as \code{earthaccess}.
}
\seealso{
\href{https://urs.earthdata.nasa.gov}{NASA Earthdata Login}
}
